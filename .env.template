# Database configuration
DB_HOST=localhost
DB_NAME=osgeo_wiki
DB_USER=username
DB_PASSWORD=somepass
DB_PORT=5432

# Wiki dump path (optional, for legacy compatibility)
WIKI_DUMP_PATH=./wiki_dump

# OpenRouter API (for extension generation with free models)
OPENROUTER_API_KEY=sk-or-v1-xxxxx

# Local LLM configuration (alternative to OpenRouter)
LLM_MODEL=gemma3:latest
OLLAMA_BASE_URL=http://localhost:11434

# LLM Temperature settings
KEYWORD_TEMPERATURE=0.3
RESPONSE_TEMPERATURE=0.7

# Debug mode
DEBUG=True
